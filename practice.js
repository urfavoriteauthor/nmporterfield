var numberArray = [];
var newArray=[];
var newNum = function(numberArray){return numberArray[i] *2};

var doSomething = function(){
    for(i=0;i<numberArray.length;i++){
       numberArray.push(newArray[i] * 2);
       console.log(numberArray);
       console.log(newArray);
    }
    return newArray;
    return numberArray;
};

doSomething();